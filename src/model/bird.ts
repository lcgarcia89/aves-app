export class Bird {
  // constructor(public id: string, public id_user: string, public image: string, public name: string, public description: string,
  //             public sightings: string, public sightings_list: Array<{id: string, idAve: string, place: string, long: number, lat: number}>) { }

  constructor(public id: string, public id_user: string, public image: string, public name: string, public description: string,
              public sightings: number) { }
}
